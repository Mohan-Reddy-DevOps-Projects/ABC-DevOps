apiVersion: v1
kind: Service
metadata:
  labels:
    app: abc-ENV-saveretirementulip-plan
  name: abc-ENV-saveretirementulip-plan
spec:
  type: ClusterIP
  ports:
  - name: abc-ENV-saveretirementulip-plan
    port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: abc-ENV-saveretirementulip-plan
