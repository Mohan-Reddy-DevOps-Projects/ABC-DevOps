apiVersion: v1
kind: Service
metadata:
  labels:
    app: abc-ENV-dha
  name: abc-ENV-dha
spec:
  type: ClusterIP
  ports:
  - name: abc-ENV-dha
    port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: abc-ENV-dha
